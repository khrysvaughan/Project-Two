CREATE DATABASE housing_db;

USE housing_db;

-- Using the Table Data Import Wizard import the files into the tables
-- before executing the following lines:

-- Import file named yearlydatanew.csv into this table, then select * to see the entries
Select * from yearlydatanew;

-- Import file named combinedData.csv into this table, then select * to see the entries
select * from combineddata;

-- Import file named yearlynew.csv into this table, then select * to see the entries
select * from yearlynew;

